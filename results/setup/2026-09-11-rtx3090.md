# RTX 3090 preparation and validation — 2026-09-11

This report records environment, GPU runtime, and released-sample validation only. No FRMD
repair, persistence, physical-success, or interference-benchmark result has
been produced on this machine yet.

## Source revision and availability

- Validated TrajMem-OT revision: `7c43f4c3f2b68128f25f2ae566d27b5b81574159`.
- RoboMME runtime: `ecf086c3be7c2223167d9bb2f6ef1f0a6e24353b`.
- At verification time the remote exposed only `main`; its tip was the
  TrajMem-OT revision above. The requested FRMD/E14-R modules, CLI scripts,
  configs, and updated `RUN_NEXT.md` were absent.

## Environment and checks

- NVIDIA RTX 3090, 24 GiB VRAM; Python 3.11.16.
- JAX 0.5.3 detected `CudaDevice(id=0)`.
- Torch 2.7.1+cu126 reported CUDA available.
- Bootstrap completed using the pinned upstream runtime.
- Local editable test installation initially timed out on a UV cache lock
  held by the concurrent bootstrap. Retrying after bootstrap completed worked.
- An existing BF16 assertion failed in NumPy 1.26's dtype promotion even
  though both arrays had identical values. The accompanying test-only patch
  casts the compared arrays to FP32, retaining the half-chord check.
- `OMP_NUM_THREADS=4 .venv/bin/python -m pytest -q`: **100 passed in 69.52s**.
- `.venv/bin/python -m compileall -q src scripts tests`: exit 0.
- `uv pip check --python .venv/bin/python`: all packages compatible.
- `git diff --check`: exit 0.
- Installed the missing system OpenGL library required by upstream OpenCV;
  the real RoboMME dataset import and sample loading then succeeded.

## Released sample

Source: `Yinpei/robomme_preprocessed_data_sample`, pinned revision
`5a8a8650f5d99bfa1a44cadbdbd492fbbf7bd0a5`.

| Archive | SHA-256 | Extracted files | Uncompressed bytes |
| --- | --- | ---: | ---: |
| data.zip | `5a0f19430067bd6fcda832c7398c7edf0f0c1721e5b52acb55bee16695cbe8b0` | 27,452 | 10,858,675,229 |
| features.zip | `8e0a4cbfacd3c31e1950f8415f651230e76f791d1bcb6f895308d398c1492318` | 42,842 | 25,784,088,779 |

Extraction completed with ZIP integrity checks, and each extracted file's
size was checked against its archive entry. Downloaded archives were removed
after verification to recover disk space; extracted data were retained.

Inventory found 27,452 sample records from 80 episodes. These are native
single-episode records, not a validated k=3/k=7 interference catalog.
No interference indices or task outcomes were fabricated from this inventory.

## Alternative model preparation

Selected released candidate: `Yinpei/mme_vla_suite/perceptual-tokendrop-modul`.
Checkpoint revision: `5db4d53ddb98c7f80cab08792dd53d985d712ab1`.
The 11,878,874,944-byte ZIP was downloaded and SHA-256 verified as
`22e9f1b660e29f17d473444b6810506278c05c66e6fc9a1ad2d8d4db1bad7f39`.
The archive's training-machine path prefix was stripped after extraction.
Upstream `RoboMMEDataset` with `perceptual-tokendrop-modul.yaml` successfully
loaded index 0: dataset length 27,452, memory shape `[512, 2048]`, mask `[512]`.

The real frozen policy then passed a four-query GPU runtime diagnostic:

- Two baseline calls with identical fixed diffusion noise were exactly equal.
- Baseline and both BF16 finite-response endpoint actions were finite.
- Memory: BF16 `[512, 2048]`; normalized internal action chunk: `[20, 32]`.
- Input half-chord norm: `0.4760815501213074`.
- Action half-chord norm: `0.005303168669342995`.
- Elapsed time including model restoration and compilation: `133.5617 s`.

Protocol: policy seed 7, sample index 0, 10 denoising steps, diffusion noise
from JAX key 108, fixed policy key 109, and a unit-Frobenius Gaussian memory
direction from key 110. Endpoint radius was `2.5e-4 * ||M||_F`; endpoint
arithmetic was FP32 followed by BF16 quantization. Raw action arrays remain
local. This establishes runtime compatibility and a finite response, not
useful repair or physical improvement. See the adjacent runtime JSON.

The benchmark at pinned commit `856bc3a189d4172f3f47dbee4424d585f8d78db3`
was also retrieved. Its task-goal definitions identify only five released
sample episodes each for PatternLock, RouteStick, and VideoPlaceButton,
versus the requested ten per task. See the adjacent coverage JSON.

## Required next inputs and checks

1. Obtain the missing FRMD commit and its actual run instructions.
2. Extend the verified alternative-model runtime diagnostic to the actual
   FRMD protocol once its implementation is available.
3. Obtain or build a provenance-validated interference catalog with the
   requested episode coverage; the released sample alone is not that catalog.
4. Run FRMD dry-run, smoke, pilot, persistence, and physical evaluation;
   expand only if the predeclared pilot criteria pass.

Large data, model files, and action archives are excluded from Git.
